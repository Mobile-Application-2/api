import {Router} from 'express';
const router = Router();

// router.get('/top/winners'); // players with most win all time for my tournaments

// router.get('/tournaments'); // get all my tournaments

// router.get('/tournaments/:tournamentId'); // get a single tournament info

// router.get('/tournaments/:tournamentId/users'); // get all participating users for a tournament

// router.get('/tournaments/:tournamentId/fixtures'); // get all fixtures for a tournament

// router.post('/tournaments');

// router.patch('/tournaments/:tournamentId/prizes')

// router.patch('/tournaments/:tournamentId/code') // allow them create the code

export default router;
